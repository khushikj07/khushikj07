#include <iostream>
using namespace std;
int main()
{
char str[100], rev[100];     
int count = 0, end, i;   
  cin.getline(str,100);
for(int i=0;str[i]!='\0';i++){
  count++;
}
  end=count;
  for(int i=0;i<count;i++){
    rev[i]=str[end-1];
    end--;
  }
std::cout<<rev;
}
