functions in string

append(): Appends a string or a part of a string to the current string.
string str = "hello";
str.append(" world");
cout << str; // Output: hello world

subtr(): Returns a substring of the string.
string str = "hello";
str.append(" world");
cout << str; // Output: hello world

find() and rfind(): Finds the first or last occurrence of a substring in the string
string str = "hello world";
size_t pos = str.find("world"); // 6

erase(): Erases a part of the string.
string str = "hello world";
str.erase(5, 6); // "hello"

compare(): Compares two strings.
string str1 = "hello";
string str2 = "world";
int result = str1.compare(str2); // returns a value < 0

#include<bits/stdc++.h>
using namespace std;
int main()
{
   char name[20];
   getline(cin,name);   /* cin.getline(name,20); now here parameters are like name where an array in which to store data and 20 define the number of data to be extracted and one more imp thing is that 
       cin doesnot take space as input but getline take space as input so we always use getline */

cout<<name;
}

for ex
Virat Arora
Virat Arora

string str1="HAI";
str1.front();     // access to first element
str1.back();    // gives access to last element of string

string fnum;     // 955.445
cin>>fnum;
int pos=fnum.find(".");
string str;
str.assign(fnum, pos+1);  // str ke pas 445 aaega . ke baad wali string assign ho jaegi

find function:- The find function returns string::npos if the substring is not found. This means that the condition should be if(pos != string::npos).









